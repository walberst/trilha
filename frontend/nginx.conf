server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA com vue-router em modo history: qualquer rota cai no index.html
    # e o roteamento acontece no cliente.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
